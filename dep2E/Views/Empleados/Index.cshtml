@model IEnumerable<dep2E.Models.Empleado>

@{
    ViewBag.Title = "Empleados";
}

<h2>Lista de Empleados</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create", null, new { @class = "btn btn-primary" })
    @Html.ActionLink("Buscar por Empleado", "BuscarPorEmpleado", "Empleados", null, new { @class = "btn btn-default" })
</p>
@{
    int i = 1;
}

<table class="table">
    @if (Model.Count() != 0)
    {
        <tr>
            <th>#</th>
            <th>
                @Html.DisplayNameFor(model => model.nombre_Empleado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Departamento.nombre_Departamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sueldo)
            </th>
            <th>Opciones</th>
        </tr>
        foreach (var item in Model)
        {
            <tr>
                <td>@i</td>
                <td>
                    @Html.DisplayFor(modelItem => item.nombre_Empleado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Departamento.nombre_Departamento)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.sueldo)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.idEmpleado }, new { @class = "btn btn-primary" })
                    @Html.ActionLink("Detalle", "Details", new { id = item.idEmpleado }, new { @class = "btn btn-default" })
                    @Html.ActionLink("Eliminar", "Delete", new { id = item.idEmpleado }, new { @class = "btn btn-warning" })
                </td>
            </tr>
            i++;
        }
    }
    else
    {
        <tr>
            <td colspan="3" align="center"><h5>No hay Empleados registrados</h5></td>
        </tr>
    }
</table>
@Html.ActionLink("Regresar a inicio", "Index", "Home")
